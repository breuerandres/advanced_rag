services:
  postgres:
    ports:
      - "5432:5432"

  dotnet-api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development

  rag-api:
    environment:
      APP_ENV: development

  caddy:
    environment:
      PUBLIC_DOMAIN: localhost

