# Advanced RAG RAG API

FastAPI service for chat, retrieval, indexing, RAG audit, semantic cache, and AI budget enforcement.

## Local Checks

```powershell
uv run pytest -q
uv run ruff check .
uv run mypy src tests
```
