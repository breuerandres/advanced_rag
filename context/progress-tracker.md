# Progress Tracker

## Current Phase

- MVP implementation and stabilization.

## Current Goal

- Review and then execute the hierarchical document-access refactor implementation plan that replaces the current flat group-only operating model with hierarchical organizational units plus transverse groups.

## Completed

- On 2026-06-11, RAG hardening Slices 1–3 of `docs/superpowers/plans/2026-06-11-rag-hardening-roadmap.md` were implemented and agent-verified on branch `rag-hardening-s1-3` (off `mvp-implementation`), commit-per-task. **Slice 1 (retrieval lifecycle correctness, C1/C2):** new `.NET` EF migration `20260611090000_GrantRagOwnerDocumentsReadAccess`; retrieval `hybrid_retrieval.py` gained a `_DOCUMENT_STATE_PREDICATE` (published-corpus chunks eligible only when `app.documents.current_state='Published'` AND `current_published_version_id = chunk.document_version_id`; preview-corpus requires non-archived) applied inside both CTEs; indexing now deactivates prior chunks by `(document_id, corpus)` instead of by version; `rag-spec.md` updated. **Slice 2 (cache invalidation, C3):** new `IInternalCacheInvalidationClient` + `FastApiInternalCacheInvalidationClient` (mirrors the indexing client, posts `documentIds` to rag-api `/internal/cache-invalidations`), wired into DI and called best-effort after archive, republish (only when a previous published version existed), restore, and edit-after-publish (`UpdateDraftAsync` on a previously published document). **Slice 3 (query-time multimodal RAG):** Alembic `20260611_130000` adds `rag.document_chunk_images` + five `multimodal_*` audit columns; the chunker now extracts stable `/api/document-images/{id}/content` references during HTML parsing (not from rebuilt chunk HTML) and indexing persists them; new internal token-protected `.NET` `GET /internal/document-images/{id}/content`; FastAPI `multimodal_images.py` selects/dedupes/caps (3 images, 5 MB, `detail=low`) and fetches authorized bytes; `OpenAILlmProvider.multimodal_complete` uses the Responses API (`store=false`, `text.format`); `chat_service.answer` selects images after final retrieval, routes to multimodal generation only when ≥1 image survives, audits the five fields, and excludes multimodal answers from the semantic-cache write. Minor deviations from the original 2026-06-01 plan (superseded by the slice delta): JSON output uses `{"type":"json_object"}` (matching the existing text path) rather than `json_schema`; image bytes are fetched sequentially to keep the total-bytes cap deterministic rather than via `asyncio.gather`. Verification: rag-api `uv run pytest -q` 69 passed, `ruff` + `mypy src tests` clean; `dotnet test AdvancedRag.sln` 137 passed (App 50 / Api 67 / Infrastructure 20); `dotnet build` clean (expected NuGet vulnerability-index warning only). User-owned Compose acceptance for all three slices is pending (see Next Up). `graphify update .` was run after implementation.

- On 2026-06-11, chat-web received a design pass on branch `docs-redesign`: the citation drawer was rebuilt as a non-modal inline right panel (third layout grid column, fixed overlay under 1100px, Esc/button close) replacing the modal `CitationDrawer` usage; feedback moved from one global bottom form to per-answer icon toggles with inline comment expansion and per-turn drafts; transcript turns render as ChatGPT-like bubbles (shared `ChatMessage` gained an optional `showAuthor` prop, default unchanged); streaming auto-scrolls while the user is pinned to the bottom; dead single-answer render branches and state were removed; the empty state, composer, sidebar, citations, and feedback strings now come from i18n resources; chat-web `index.css` lost stale docs-web warm-theme overrides that broke dark mode on the body. Verified: chat-web 23 tests, shared-ui 32 tests, typecheck, lint, and production build all green. Decision logged in `design-decisions.md` (2026-06-11). User-owned browser verification of the panel/feedback flows is pending.
- On 2026-06-11, the docs-web redesign + document-scoped mini chat implementation plan (11 TDD tasks) was implemented and agent-verified on branch `docs-redesign` (off `roles-refactor`). Backend (rag-api): new `scope_document_id` audit column + Alembic migration; `HybridRetrievalParams.scope_document_id` filters both retrieval CTEs; `ChatService.answer` accepts `documentId`, forces the published corpus when scoped, bypasses semantic-cache lookup and write, audits `scope_document_id`, and excludes doc-scoped turns from `list_sessions`. Infra: Caddy `docs.{$PUBLIC_DOMAIN}` routes `/api/chat` + `/api/feedback*` to rag-api before the .NET `/api/*` catch-all. Frontend (docs-web): split the monolithic `App.tsx` into `features/portal`, `features/auth`, `features/viewer`, `features/docChat`; shared `lib/` helpers; full i18n error-code keys; warm help-center portal with document-type filter chips; reading-first viewer with back-to-library nav and draft/review banner; ephemeral floating doc-scoped chat (SSE client copied from chat-web) with per-answer feedback, budget/rate-limit states, and partial-turn rollback + retry. One plan deviation: `DocumentView`/`DocumentCard` render the dynamic lucide icon via `createElement(typeIcon(...), ...)` instead of `const Icon = ...; <Icon/>` to satisfy `eslint-plugin-react-hooks@7`'s `react-hooks/static-components` rule. Agent verification: rag-api `uv run pytest -q` 54 passed, `ruff` + `mypy` clean; docs-web `App.test.tsx` 19 passed, typecheck/build/lint clean; Compose config validates. User-owned browser/Compose acceptance (spec Â§9 matrix) is pending.
- On 2026-06-10, the docs-web redesign + document-scoped mini chat design was approved and documented. The spec lives at `docs/superpowers/specs/2026-06-10-docs-web-redesign-and-doc-chat-design.md` (warm help-center portal/viewer restyle, back-to-library navigation, ephemeral floating doc-scoped chat with streaming + feedback, Caddy docs-host routing to FastAPI, cache bypass, `scope_document_id` audit, session-list exclusion). The implementation plan with 11 TDD tasks is at `docs/superpowers/plans/2026-06-10-docs-web-redesign-and-doc-chat-implementation-plan.md`. The routing/cache/session-list decisions were recorded in `context/design-decisions.md`.
- On 2026-06-08, the hierarchical document-access refactor implementation plan was written at `docs/superpowers/plans/2026-06-08-hierarchical-access-refactor-implementation-plan.md`. The plan covers the EF schema and seed reset, effective access-scope contract, scoped document lifecycle authorization, .NET API contracts, FastAPI branch-aware RAG filtering, management UI updates, user-owned destructive reset checkpoint, browser acceptance matrix, verification, and context-update requirements. No schema, code, database reset, or seed execution has started.
- On 2026-06-08, Task 1 of the hierarchical document-access refactor was implemented and verified. The `.NET` EF model now maps organizational units, organizational-unit closure rows, user organizational-unit assignment, user access-scope version, group ownership/publishing policy, document permission group joins, and publisher-to-group grants. The append-only EF migration `20260608100000_AddHierarchicalAccessBaseline` adds the hierarchy baseline, root `Empresa`, new roles `DocumentEditor` and `DocumentPublisher`, and read grants for the RAG owner. The migration is defensive for legacy partial-schema upgrade tests while preserving the normal clean-schema contract. `Seed-LocalDemoData.ps1` was rewritten to seed the approved hierarchy, groups, users, and published demo documents with deterministic IDs. Verification passed with `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~AppDbContextMappingTests|FullyQualifiedName~AppDbContextMigrationTests|FullyQualifiedName~SetupEndpointTests"` and `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`; the build still emits the existing NuGet vulnerability-index warning when nuget.org is unreachable.
- On 2026-06-08, Task 2 of the hierarchical document-access refactor was implemented and verified. `.NET` now exposes an app-layer `EffectiveAccessScope` contract, computes `AccessScopeHash` V2 from primary role, `isGlobalAdmin`, organizational-unit id, sorted group ids, and `accessScopeVersion`, and returns hierarchical scope claims from internal session validation. Role, group, and active-status changes increment `users.access_scope_version`; organizational-unit assignment changes must use the same rule when the later user API slice adds that mutation. FastAPI now parses `isGlobalAdmin`, `organizationalUnitId`, and `accessScopeVersion`, and `DotnetSessionValidator` fetches `.NET` validation on every request instead of caching access-relevant claims. Verification passed with `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~AuthEndpointTests|FullyQualifiedName~UserAdministrationEndpointTests"`, `uv run pytest tests/test_session_validation.py tests/test_auth_tokens.py -q`, `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~UserAdministrationServiceTests"`, `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`, and `uv run ruff check src/advanced_rag/auth tests/test_session_validation.py tests/test_auth_tokens.py`; the .NET build still emits the existing NuGet vulnerability-index warning when nuget.org is unreachable.
- On 2026-06-08, Task 3 of the hierarchical document-access refactor was implemented and verified. The `.NET` document app layer now uses `DocumentAccessRuleDraft` and `DocumentAccessRuleRecord` instead of group-id-only domain state, while keeping derived `AllowedGroupIds` for legacy API/UI compatibility until later slices replace public DTOs. A central `DocumentAccessPolicy` enforces published-document read rules, scoped draft management, scoped publishing, organizational-unit branch checks, group ownership policies, explicit publish grants, and invalid empty-rule rejection. Document lifecycle, viewer document loading, viewer catalog listing, viewer handoff link authorization, document-image content reads, and document-image uploads now route through the central policy when effective-access services are available. EF persistence now stores and loads organizational-unit document rules plus `document_permission_groups`, with defensive legacy `group_id` read compatibility. Verification passed with `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~DocumentLifecycleServiceTests|FullyQualifiedName~ViewerAccessServiceTests|FullyQualifiedName~ViewerDocumentCatalogServiceTests|FullyQualifiedName~EfDocumentRepositoryTests"`, `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`, and `git diff --check` with Windows line-ending warnings only. The build still emits the existing NuGet vulnerability-index warning when nuget.org is unreachable.
- On 2026-06-08, Task 4 of the hierarchical document-access refactor was implemented and verified. The `.NET` API now exposes `GET/POST/PATCH /api/organizational-units`, create-user requests require `organizationalUnitId`, user responses include `organizationalUnit`, group responses include ownership and `publishingPolicy`, and document create/update requests accept `accessRules` while keeping `allowedGroupIds` only as a transition compatibility path. Document create/update now reject missing or empty access rules immediately. Public API authorization was moved off the legacy `DocumentManager` role and onto `Admin`, `DocumentEditor`, and `DocumentPublisher` as appropriate; historical migrations still contain `DocumentManager` only as migration history. Verification passed with `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~UserAdministrationEndpointTests|FullyQualifiedName~Document"`, `dotnet test services\dotnet-api\AdvancedRag.sln --filter "FullyQualifiedName~OrganizationalUnitsEndpointTests|FullyQualifiedName~UserAdministrationEndpointTests|FullyQualifiedName~DocumentEndpointTests"`, and `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`; the build still emits the existing NuGet vulnerability-index warning when nuget.org is unreachable.
- On 2026-06-08, Task 5 of the hierarchical document-access refactor (FastAPI RAG branch-aware filtering) was implemented as an agent-owned change; user-owned verification is pending. `services/rag-api/src/advanced_rag/rag/hybrid_retrieval.py` replaces the group-only permission `EXISTS` clause with a branch-aware access predicate applied inside both retrieval CTEs: a global admin bypasses the filter; otherwise a document matches when any access rule matches (OR between rules), with the organizational-unit condition (the company-wide root rule, or ancestor/descendant via `app.organizational_unit_closure`) AND the group condition (via `app.document_permission_groups`) both required within a rule, and rules with neither unit nor group ignored defensively. `HybridRetrievalParams` and `ChatService` now pass `is_global_admin`, `user_organizational_unit_id`, `root_organizational_unit_id` (Empresa = `01000000-0000-0000-0000-000000000001`), and `user_groups`; the previous no-groups short circuit was removed so users with only organizational-unit or company-wide access still retrieve. The dimension-value filter and the v2 `access_scope_hash` semantic-cache partitioning are unchanged (v1 cache entries cannot match v2 scopes because the hash differs). `tests/test_chat_rag.py` migrates its fixture to the new `app` schema (organizational units, closure, `document_permission_groups`, `document_permissions.organizational_unit_id`) and adds direct-`hybrid_retrieve` tests for ancestor/descendant vs sibling visibility, group-membership requirement plus empty-rule rejection, and the admin bypass. Pending user verification: `Set-Location services\rag-api; uv run pytest tests/test_chat_rag.py tests/test_session_validation.py tests/test_migrations.py -q; uv run ruff check .; Set-Location ..\..`.
- On 2026-06-08, Task 6 of the hierarchical document-access refactor (management UI) was implemented as an agent-owned change; user-owned verification is pending. `apps/manage-web` adds an `api/orgUnits.ts` client; `api/documents.ts` adds `accessRules` (request + response) while keeping `allowedGroupIds` on responses for the list view; `api/users.ts` exposes the user `organizationalUnit`, group `ownerOrganizationalUnit`/`publishingPolicy`, and a required `organizationalUnitId` on create. `UsersBudgetPage` replaces the `DocumentManager` role option with `DocumentEditor`/`DocumentPublisher`, adds a required organizational-unit selector to the create-user dialog (lazy-loaded; the edit dialog shows the unit read-only because no change-unit endpoint exists in this slice), adds an organizational-unit column, and shows owner-unit and publishing-policy columns in the groups tab. `DocumentsPage` replaces the flat access-group checkbox list with an access-rule editor (one organizational unit plus zero or more groups per rule, root Empresa labelled company-wide, inline empty-rule validation, and backend `VALIDATION_FAILED` surfaced as a Spanish error), sends `accessRules` on save, and maps publish/archive/retry authority onto `DocumentPublisher`. Role gating in `App.tsx` and `ManagementNav.tsx` and the login copy moved off `DocumentManager`. `App.test.tsx` now intercepts `/api/organizational-units` out of band (so the lazy fetch never disturbs the ordered fetch queue), updates the role/access tests, and adds the three required new tests (create user with one organizational unit and multiple groups; document editor saves organizational-unit and group access rules; document editor surfaces invalid empty access-rule errors). Two pre-existing TipTap toolbar tests that still expect the old `Estilo de bloque` combobox remain red independently of this work. Pending user verification: `pnpm.cmd --dir apps\manage-web test -- --run src/App.test.tsx; pnpm.cmd --dir apps\manage-web typecheck; pnpm.cmd --dir apps\manage-web build`.
- On 2026-06-10, the remaining documents access-rules UX backlog was implemented and agent-verified on branch `roles-refactor` (second pass): natural-language access previews per rule with an OR separator pill and branch-inheritance hints, unit and group list filters replacing the legacy with/without-groups filter (search now also matches unit and rule-group names), a per-rule group search input with selected counter, per-rule group creation, pinned options preserving unit ids missing from the catalog, duplicate-rule and company-wide-redundancy warnings, stable rule keys, and numbered remove-rule labels; the rule editor was extracted into an `AccessRuleCard` component and the AND/OR help text was reworded in both locales. Verification: typecheck clean; `App.test.tsx` 51 tests with 49 passing and only the two pre-existing TipTap "Estilo de bloque" failures; lint findings unchanged and pre-existing. Only the group-ownership scoping UI remains, blocked on backend per-user usable-group data; details in `docs/plans/2026-06-10-documents-access-rules-ui-handoff.md`.
- On 2026-06-10, a UX review of the documents access-rules UI produced three agent-verified improvements on branch `roles-refactor`: the documents list Access column now resolves real organizational-unit names (it previously labelled every unit-scoped rule "Toda la empresa" because the list never loaded the unit catalog; unresolved ids render a "unit unavailable" label), empty new access rules show a neutral hint instead of an immediate red error until a validation attempt fails, and the editor's unit select is indented by depth in tree order with units passed down from the page (the editor's own lazy unit fetch was removed). Verification: `apps/manage-web` typecheck clean, `App.test.tsx` 48 tests with only the two pre-existing TipTap "Estilo de bloque" failures (confirmed pre-existing via stash comparison), lint findings all pre-existing in untouched files. The remaining UX backlog (visible AND/OR semantics with natural-language rule previews, unit/group list filters replacing the legacy with/without-groups filter, scalable group picker, per-rule group creation, inactive-unit preservation, duplicate-rule warnings) is recorded in `docs/plans/2026-06-10-documents-access-rules-ui-handoff.md`.
- On 2026-06-10, the access-model management UI was completed and agent-verified on branch `roles-refactor`. Surfaces delivered: Admin-only Organizational Units tree editor page (create child unit / rename / activate-deactivate; no branch move or delete, per backend limits); `GET /api/organizational-units?includeInactive=true` restricted to Admin callers; group owner-unit and publishing-policy editing in the group create/edit dialogs; documents list Access column now summarizes access rules per unit (company-wide rules no longer show "-"); new `PUT /api/users/{id}/organizational-unit` endpoint (Admin only) that bumps `users.access_scope_version`, wired into the user management dialog. Agent verification passed: backend filter `OrganizationalUnitsEndpointTests|UserAdministrationEndpointTests|EfUserAdministrationRepositoryTests` â€” 21 passed (Api.Tests) + 1 passed (Infrastructure.Tests), 0 failed; `apps/manage-web` typecheck clean; production build succeeded; `App.test.tsx` 45 passed / 2 failed (both pre-existing `Estilo de bloque` TipTap tests, out of scope). User-owned browser/Compose acceptance (Task 7 destructive reset + acceptance matrix) is still pending.
- On 2026-06-10, organizational-unit level differentiation was added to the management UI on branch `roles-refactor` (frontend-only, no backend change). A shared `UnitLevelBadge` (numbered `N{level}` chip, accessible-name-hidden, tinted by `level % 6` with dark-mode tones) is the single source of truth for level visuals across the Organizational Units tree page and the unit dropdowns. The shared-ui `Select` was extended with optional per-option `depth` (indentation) and `badge` (leading adornment, also shown in the closed trigger) while staying backward-compatible. All four `UsersBudgetPage` unit dropdowns (create/edit user, create/edit group) plus the document editor's per-rule access-unit dropdown (`AccessRuleCard` in `DocumentsPage`) migrated from native `<select>` to the Radix `Select` with depth indentation and level badges, ordered via a new `flattenUnitsInTreeOrder` helper (DFS tree order with computed level) or the existing `sortUnitsInTreeOrder`; group and rule dropdowns keep their "no owner unit" / "no unit" choice via a sentinel value since Radix forbids empty-string items (the rule dropdown also preserves an unresolved unit id as a pinned option). The Organizational Units tree rows also gained a `var(--bg-subtle)` hover band (theme-aware) so the right-side action buttons read against their level. Manage-web test setup gained the Radix jsdom polyfills (`ResizeObserver`, pointer capture, `scrollIntoView`). Verification: shared-ui typecheck + 32 tests pass (incl. new badge/indentation test); manage-web typecheck clean; `App.test.tsx` 50 passed / 2 failed (the same pre-existing `Estilo de bloque` TipTap tests, confirmed pre-existing via setup stash comparison); lint findings all pre-existing in untouched files (verified the `OrganizationalUnitsPage` `loadUnits` finding against HEAD).
- On 2026-06-10, the unit dropdowns were upgraded from the indented Radix `Select` to a collapsible tree picker on branch `roles-refactor` (frontend-only), at user request, to mirror the Organizational Units page. New `UnitTreeSelect` (manage-web) renders a select-like trigger plus a popover tree with expand/collapse chevrons, level badges, and a check on the selected row; it opens collapsed to its roots and closes on select. It is built on the shared-ui `Popover`, which was extended (backward-compatibly) with controlled `open`/`onOpenChange`, `align`, and `contentClassName`. All five unit dropdowns (create/edit user, create/edit group, and the document editor's per-rule access unit) now use `UnitTreeSelect`; the "no unit"/"no owner unit" choices and an unresolved-unit id are passed as fixed `leadingOptions` above the tree. `unitTree.ts` now exposes `buildUnitTree` (parentâ†’children map with orphan promotion); the prior `flattenUnitsInTreeOrder`/`unitSelectOptions` helpers were removed. The shared-ui `Select` `depth`/`badge` extension and its test remain (generic capability) but are no longer consumed by the app. New i18n keys `orgUnits.expand_branch`/`collapse_branch`. Verification: shared-ui typecheck + 32 tests pass (Popover/Select included); manage-web typecheck clean; `App.test.tsx` 50 passed / 2 failed (same pre-existing `Estilo de bloque` TipTap tests); lint findings all pre-existing in untouched files. The test helper now opens the trigger, expands named branches, then clicks the option (tree rows are buttons).

- On 2026-06-05, `manage-web` sidebar cross-app navigation was added for authenticated users. The management sidebar now includes external `Chat` and `Docs` links with lucide icons, localized labels, and host-derived sibling URLs such as `https://chat.localhost/`, `https://docs.localhost/`, `https://chat.<domain>/`, and `https://docs.<domain>/`. Verification passed with the focused management navigation test, `pnpm.cmd --dir apps\manage-web typecheck`, `pnpm.cmd --dir apps\manage-web build`, and `git diff --check` with Windows line-ending warnings only. The full `src/App.test.tsx` run remains blocked by two pre-existing toolbar expectation failures that still look for the old `Estilo de bloque` combobox after the toolbar was changed to fixed `P/H1/H2/H3` buttons.

- On 2026-06-05, follow-up chat UI polish was implemented after user review. Duplicate cache-hit citations are deduplicated by document/version, answer citations now open from a deduplicated `Ver citas (n)` control into a fixed-height right drawer, feedback buttons can be selected or deselected before submission, the composer starts as a single-line autosizing textarea, chat login now uses the same auth-card stack and visible language/theme controls as management, the fixed left drawer shows active session user context, and chat UI mojibake around `cachÃ©`/session/login labels was corrected. Verification passed with `pnpm.cmd --dir packages\shared-ui test -- --run src/components/ChatComposer.test.tsx src/components/CitationDrawer.test.tsx`, `pnpm.cmd --dir apps\chat-web test -- --run src/App.test.tsx`, `uv run pytest tests/test_chat_rag.py -q`, `pnpm.cmd --dir apps\chat-web typecheck`, `pnpm.cmd --dir packages\shared-ui typecheck`, `pnpm.cmd --dir apps\chat-web build`, and `git diff --check` with Windows line-ending warnings only. The App test run still emits the existing Vitest warning about `--localstorage-file`.
- On 2026-06-05, the `chat-web` redesign was implemented after user approval without a separate written spec per explicit user request. The app now uses a fixed management-style left drawer for conversation history, new conversation, language selection, dark-mode toggle, and logout. The main chat area is simplified around the active transcript, usage, feedback, error states, answer-level citation access, and the bottom composer. Verification passed with `pnpm.cmd --dir apps\chat-web test -- --run src/App.test.tsx`, `pnpm.cmd --dir apps\chat-web typecheck`, `pnpm.cmd --dir apps\chat-web build`, and `git diff --check` with Windows line-ending warnings only. A local Vite server was started at `http://localhost:5174` for visual inspection; Compose/Caddy remains the right path for authenticated chat acceptance.
- The MVP implementation has progressed through the repository baseline, Compose/Caddy infrastructure, .NET API, FastAPI RAG service, React frontends, database migrations, authentication/session handling, users/groups/budgets, document lifecycle/imports, internal indexing, chat/RAG, feedback/reporting, document viewer, operational hardening, and E2E coverage.
- The product currently uses unified session-authenticated browser flows with document-id viewer locators; legacy browser chat-token and viewer-exchange runtime flows are removed from the active implementation.
- Role behavior is governed by `pruebas.md`: Viewers have limited management self-service access; DocumentManagers can inspect users and balances, create/edit groups, and assign user groups without Admin-only mutations; Admins retain full management authority.
- Runtime UI locales are limited to `es-AR` and `en-US`.
- Management role-matrix implementation and E2E coverage were added on 2026-05-31.
- Management Documents and TipTap editor i18n coverage was completed on 2026-05-31.
- On 2026-05-31, obsolete refactor documentation was removed from `docs/`, `context/`, and the root handoff file. Current implementation files and current context are now the project source of truth.
- On 2026-05-31, the first document image slice was implemented. Document image bytes are stored in private S3-compatible object storage, image metadata is stored in `app.document_images`, canonical HTML references stable `/api/document-images/{imageId}/content` URLs, and external/base64 image sources are rejected with `DOCUMENT_IMAGE_SOURCE_INVALID`.
- Verification for the first document image slice passed on 2026-05-31: focused .NET lifecycle, API, health, EF mapping/migration tests; `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`; `pnpm --dir apps\manage-web typecheck`; focused manage-web tests; manage-web production build; `docker compose --env-file infra/compose/.env.example -f infra/compose/compose.yaml config`; and `git diff --check` with line-ending warnings only.
- On 2026-06-01, user-owned local validation confirmed document images save correctly to MinIO and are visible in the MinIO browser.
- On 2026-06-01, the Raspberry Pi demo-host setup checkpoint was validated by the user: Ubuntu Server is running on `aarch64`, Docker Engine reports `arm64` server architecture, Docker Compose is installed, and UFW allows OpenSSH plus ports 80 and 443.
- On 2026-06-01, FastAPI text-first RAG image indexing was implemented. The chunker indexes accessible image text from saved HTML (`alt`, then `aria-label`, then `title`) plus nearby captions, without indexing image URLs, fetching object bytes, or using multimodal OpenAI image inputs. Verification passed with `uv run pytest tests/test_chunking.py tests/test_indexing.py -q`, `uv run ruff check .`, `uv run pytest -q`, and `uv run mypy src tests` from `services/rag-api`; repository `git diff --check` returned only line-ending warnings.
- On 2026-06-01, a management editor regression was fixed for published documents. Database inspection confirmed published documents had successful RAG indexing jobs and persisted chunks; the bug was isolated to `manage-web` initializing the editor only from `currentDraftVersion`, which is null after publication by design. The editor now falls back to `currentPublishedVersion` when no draft exists, so opening a published document shows the published title, metadata, and HTML content before creating the next draft. Verification passed with a red/green regression test, `pnpm.cmd --dir apps\manage-web typecheck`, `pnpm.cmd --dir apps\manage-web test -- --run`, `pnpm.cmd --dir apps\manage-web build`, and `git diff --check` with line-ending warnings only. A separate `pnpm.cmd --dir apps\manage-web lint` run remains blocked by pre-existing lint findings in untouched files: `ManagementNav.tsx`, `AuditPage.tsx`, and `ConfigurationPage.tsx`.
- On 2026-06-01, the user approved query-time multimodal RAG as the next image strategy. The approved design keeps textual retrieval first, attaches only a capped set of authorized images associated with retrieved chunks, fetches image bytes through a `.NET` internal endpoint, uses OpenAI Responses API for multimodal generation, and disables semantic cache writes for multimodal answers.
- On 2026-06-01, Caddy demo-host routing was adjusted to use `PUBLIC_DOMAIN` in the frontend site labels instead of hardcoded `.localhost` hosts, so a Raspberry Pi demo host can be reached from another machine through explicit DNS/hosts entries while preserving same-origin routing.
- On 2026-06-01, the Raspberry Pi demo stack was reachable from the Windows client through `manage.ragpi.lan`, `chat.ragpi.lan`, and `docs.ragpi.lan`. The user logged in with the seeded admin account across the services and changed the default password.
- On 2026-06-02, the user confirmed the Raspberry Pi demo stack works end to end after the admin password change.
- On 2026-06-02, the user confirmed ownership of `breuerai.com`; the Raspberry Pi demo should use `manage.breuerai.com`, `chat.breuerai.com`, and `docs.breuerai.com` through Cloudflare Tunnel.
- On 2026-06-03, Raspberry Pi + Cloudflare Tunnel demo deployment notes were documented in `docs/operations/raspberry-pi-cloudflare-demo.md`, including public hostnames, `cloudflared` setup, HTTP local origin routing, verification commands, and troubleshooting for Cloudflare 502/TLS origin errors.
- On 2026-06-03, an interim manual update helper was added at `updateService.sh` with documentation in `docs/operations/manual-service-update.md`. It performs a safe source-based update for the demo host until GHCR/CD is implemented: tracked-worktree guard, optional environment selection, Postgres backup, fast-forward-only pull, Compose validation, rebuild/recreate, health waits, and recent log output.
- On 2026-06-03, a host reboot recovery helper was added at `installServiceAutostart.sh` with documentation in `docs/operations/systemd-autostart.md`. It installs `advanced-rag.service` as a systemd oneshot unit that runs Docker Compose after Docker and network-online are available.
- On 2026-06-03, unauthenticated management surfaces gained visible language and dark-mode controls through the shared `AuthFrame`, including the login page. Login copy is now backed by `es-AR` and `en-US` i18n keys. Verification passed with the focused manage-web login-control test, `pnpm.cmd --dir apps\manage-web typecheck`, and `pnpm.cmd --dir apps\manage-web build`. A full `src/App.test.tsx` run remains blocked by two pre-existing authenticated language-switch tests that still render Spanish after selecting English.
- On 2026-06-03, the Raspberry Pi/demo host branch was realigned with `origin/mvp-implementation` after temporary local Caddy and file-permission commits were discarded from the deployment branch. The remaining local `backups/` directory is untracked deployment-host data and should be ignored locally through `.git/info/exclude` or a later repository-level `.gitignore` update.
- On 2026-06-04, document creation/editor fixes were implemented and agent-verified: management document editor quick group creation, richer TipTap toolbar and list/content styling, DOCX/PDF import responses with safe `contentHtml` plus fallback text, Mammoth `1.11.0` DOCX conversion without embedded image import, DB-backed one-time viewer session handoff codes, docs-web handoff consumption with URL cleanup, and viewer content list styling. Verification passed with focused .NET viewer/import/migration tests, .NET solution build, manage-web App tests/typecheck/build, docs-web App tests/typecheck/build, and `git diff --check` with line-ending warnings only. User-owned Compose/browser acceptance remains pending.
- On 2026-06-04, follow-up management editor usability fixes were implemented and agent-verified: document editor group creation now uses the same modal workflow as the users/groups workspace, access groups render in a compact three-column grid, a select-all groups action was added, the TipTap packages were refreshed from `3.23.5` to the npm latest `3.25.0`, and the toolbar was refit into the compact grouped layout with a native color input backed by the official TipTap color/text-style extensions plus a clear-color action. The document HTML sanitizer now preserves only the `color` CSS property and strips other inline CSS. Manage and chat document-viewer handoff links now open `docs-web` in a new tab with `noopener,noreferrer`. A Docker/Compose frozen-lockfile mismatch was corrected by regenerating the `apps/manage-web` pnpm lockfile importer so every TipTap specifier matches the exact `3.25.0` manifest entries. Verification passed with focused manage-web and chat-web App tests, full manage-web and chat-web App test files, `pnpm.cmd --dir apps\manage-web install --frozen-lockfile`, `pnpm.cmd --dir apps\manage-web typecheck`, `pnpm.cmd --dir apps\chat-web typecheck`, `pnpm.cmd --dir apps\manage-web build`, `pnpm.cmd --dir apps\chat-web build`, focused .NET sanitizer tests, and `dotnet build services\dotnet-api\AdvancedRag.sln --no-restore`.
- On 2026-06-04, a toolbar review follow-up refined the management TipTap toolbar without browser automation: the heading selector was replaced by fixed `P`, `H1`, `H2`, and `H3` buttons, semantic highlight and text-alignment controls were added, docs rendering now styles `<mark>`, and the document sanitizer preserves only approved `color` and `text-align` CSS plus `<mark>` while continuing to strip unsafe inline styles. Verification was intentionally limited per user request and passed with `pnpm.cmd --dir apps\manage-web install --frozen-lockfile`, `pnpm.cmd --dir apps\manage-web typecheck`, focused `DocumentHtmlSanitizerTests`, `pnpm.cmd --dir apps\manage-web build`, and `git diff --check` with line-ending warnings only.
- On 2026-06-04, Chat Conversation Slice A was implemented and agent-verified. FastAPI now exposes user-scoped `GET /api/chat/sessions` and `GET /api/chat/sessions/{session_id}`, persists `session_id`, `previous_event_id`, and `rewritten_question` on RAG audit rows, and rewrites follow-up questions from bounded same-user session history before embedding/retrieval/cache. `chat-web` now loads persisted conversations after session validation, renders selected transcripts, sends a stable `sessionId` on each chat request, and keeps citations/feedback/usage attached to the assistant turn that produced them. Verification passed with `uv run pytest tests/test_chat_rag.py tests/test_locale_support.py -q`, `uv run ruff check .`, `pnpm.cmd --dir apps\chat-web typecheck`, `pnpm.cmd --dir apps\chat-web test -- --run src/App.test.tsx`, `pnpm.cmd --dir apps\chat-web build`, and `git diff --check` with line-ending warnings only.
- On 2026-06-05, Chat Conversation Slice B was implemented and agent-verified. `chat-web` now consumes `/api/chat` SSE response bodies incrementally, renders `answer-token` deltas into the pending transcript turn before `done`, keeps citations, usage, feedback controls, and session-list refresh gated on stream completion, preserves a stable local turn key across the pending-to-completed transition, and removes partial pending turns if the SSE stream fails before `done`. Verification passed with `pnpm.cmd --dir apps\chat-web test -- --run src/App.test.tsx`, `pnpm.cmd --dir apps\chat-web typecheck`, `pnpm.cmd --dir apps\chat-web build`, and `git diff --check` with line-ending warnings only. The App test run still emits the existing Vitest warning about `--localstorage-file`.
- On 2026-06-05, management sidebar product links were implemented and agent-verified. `manage-web` now shows Chat and Docs links in the sidebar, opens them in a new tab, keeps the sidebar fixed to viewport height without an internal scroll container, and creates a short-lived one-time session handoff scoped to `chat` or `docs` before opening the target. `.NET` owns the new general session handoff endpoints and `app.session_handoff_codes` persistence. `chat-web` and `docs-web` consume `?handoff=` on boot, remove it from the URL, and continue with their own host-only `__Host-session` cookie instead of showing login. Verification passed with focused .NET handoff/mapping tests, focused manage/chat/docs App tests, typechecks for all three apps, and production builds for all three apps. Browser/Compose acceptance remains pending.

## In Progress

- Hierarchical document-access Tasks 1â€“6 are implemented and agent-verified. Task 7 (user-owned destructive local reset plus the cross-surface browser acceptance matrix) is next; no destructive local database reset has been run.
- Query-time multimodal RAG design is approved and documented in `docs/superpowers/specs/2026-06-01-query-time-multimodal-rag-design.md`.
- Query-time multimodal RAG implementation plan is written in `docs/superpowers/plans/2026-06-01-query-time-multimodal-rag-implementation-plan.md`.
- Demo knowledge-library design is being drafted for a generic internal services company. The user selected this domain on 2026-06-03; final taxonomy, document set, permissions, and golden chat questions are pending approval before any seeding implementation.

## Next Up

- Agent-owned next: execute `docs/superpowers/plans/2026-06-10-docs-web-redesign-and-doc-chat-implementation-plan.md` task by task on a `docs-redesign` branch off `roles-refactor` (Docker required for rag-api Testcontainers). After implementation, the user runs the browser acceptance in spec Â§9.
- User-owned next: run Task 7 â€” the destructive local reset `docker compose --env-file infra/compose/.env.example -f infra/compose/compose.yaml -f infra/compose/compose.override.yaml down -v`, then `.\infra\compose\Start-Local.ps1 -ForceRecreate` and `.\infra\compose\Seed-LocalDemoData.ps1`, then the browser acceptance matrix in `docs/superpowers/plans/2026-06-08-hierarchical-access-refactor-implementation-plan.md` (Task 7 Step 5). FastAPI RAG filtering (Task 5) and management UI (Task 6) are both agent-verified; Tasks 5 and 6 user-owned verification commands are no longer needed separately.
- Agent-owned next (after the user confirms the above is green): finalize Task 8 by recording the verified hierarchical-access behavior in `architecture.md`, `rag-spec.md`, and `ui-context.md`, and create the scoped commit if the user wants the branch committed.
- User-owned next: run browser acceptance through the Compose/Caddy stack by logging into `https://manage.localhost`, clicking the sidebar Chat and Docs links, and confirming each opens in a new tab without showing login. Also confirm the management sidebar stays fixed-height without its own scroll while the workspace scrolls normally.
- User-owned next: run chat browser acceptance by opening `chat.localhost` through the Compose/Caddy stack, confirming the fixed left drawer appears with history, new conversation, ES/EN, dark mode, logout, and active-session user context; ask two follow-up questions in one conversation; confirm answer text appears progressively on a longer response; open `Ver citas (n)` and confirm citations appear in the fixed right drawer without duplicate cards; confirm feedback buttons can be selected and deselected before submission; reload the page and confirm that the left drawer plus selected transcript persist. If the SSE stream drops mid-answer, confirm the partial answer is removed and a retryable error is shown.
- Agent-owned later: resume the query-time multimodal RAG plan slice by slice, starting with RAG schema/indexing for `chunk -> image_id` references.
- User-owned later: after implementation, run a local Compose/browser acceptance with a published document whose image contains visual evidence not repeated in surrounding text.
- User-owned later: copy or clone the repository to the Raspberry Pi demo host, create demo-only Compose secret files locally on that host, and run the Compose stack acceptance check.
- User-owned next: when manually updating the demo host before GHCR/CD, run `chmod +x updateService.sh` once and then `./updateService.sh --env-file infra/compose/.env.pi` from the repository root.
- User-owned next: to recover after host reboot, run `chmod +x installServiceAutostart.sh` once and then `./installServiceAutostart.sh --env-file infra/compose/.env.pi` on the Linux demo host; verify with `sudo systemctl status advanced-rag.service --no-pager`.
- User-owned next: on the demo host, ignore generated backup output locally with `echo "backups/" >> .git/info/exclude` and prevent Linux file-mode noise with `git config core.fileMode false`.
- User-owned next: finish verifying public access for `chat.breuerai.com` and `docs.breuerai.com`, then optionally add Cloudflare Access in front of `manage.breuerai.com` before showing the demo to untrusted users.
- User-owned later: run the local Compose/browser acceptance for document creation/editor fixes from `docs/superpowers/plans/2026-06-04-document-creation-editor-fixes-implementation-plan.md`, including confirmation that manage-to-docs and chat-to-docs viewer links open in a new browser tab.

## Open Questions

- Hierarchical access refactor Task 5 must replace the remaining group-only FastAPI retrieval SQL before UI rule editing work starts.
- Future cleanup may add orphan image cleanup for uploaded draft images that are removed from HTML before publication.
- Demo content design must still choose the final group/access taxonomy, document type taxonomy, initial published/draft/review mix, and the golden question set for chat validation.
- No open product decisions remain for the current document creation/editor fixes slice or its management editor usability follow-up. Browser/Compose acceptance, including the new-tab document handoff behavior, is still pending.

## Architecture Decisions

See `context/architecture.md`, `context/code-standards.md`, `context/rag-spec.md`, `context/ui-context.md`, and `context/design-decisions.md`. This tracker records status only.

## Session Notes

- Conversation can continue in Spanish, but project artifacts must stay in English.
- Obsolete refactor docs and branches should not be used to infer future product behavior.

## Handoff For Next Session

Start by reading `context/README.md`. It defines reading order and source-of-truth precedence. Then read, in order:

- `context/project-overview.md`
- `context/architecture.md`
- `context/code-standards.md`
- `context/rag-spec.md`
- `context/ui-context.md`
- `context/code-patterns.md`
- `context/progress-tracker.md`
- `context/design-decisions.md`
- `context/ai-workflow-rules.md`

Current state:

- A deep rag-api review (2026-06-11) produced a seven-slice hardening roadmap: docs/superpowers/plans/2026-06-11-rag-hardening-roadmap.md. Slices 1-2 fix retrieval lifecycle correctness and missing cache invalidation, slice 3 executes the approved query-time multimodal plan plus its delta file, slices 4-7 cover retrieval/cache scale, maintenance, and true SSE streaming. Slices 1-5 are implemented and agent-verified on branch `rag-hardening-s1-3` (Compose acceptance pending); slice 4 added a materialized allowed-documents CTE, per-query `hnsw.ef_search`/iterative-scan, and partial per-corpus HNSW indexes; slice 5 made the semantic cache lookup a pgvector nearest-neighbor query (HNSW on `question_embedding`), stored and replays original citations verbatim, and filters both cache and retrieval by the configured `embedding_model`. Slice 6 added a token-protected internal maintenance purge endpoint (`POST /internal/maintenance/purge`) that deletes old inactive chunks (retention `RAG_INACTIVE_CHUNK_RETENTION_DAYS`, default 7) and expired cache entries, plus a maintenance runbook; E5's per-user budget index already existed (`ix_query_audit_events_user_created_at`), so that task was skipped as redundant. Slice 7 replaced the simulated SSE replay with true token streaming: the chat text path consumes `ILlmProvider.chat_stream` through an incremental `AnswerStreamParser` and emits `answer-token` deltas as they arrive (cache-hit/no-results/multimodal still emit a single whole-answer token); the router streams from `ChatService.answer_stream` with a pre-stream `precheck` (validation + budget) that keeps returning JSON error envelopes. All seven RAG hardening slices are now implemented and agent-verified on branch `rag-hardening-s1-3`; Compose/manual acceptance for the suite remains user-owned.

- The active branch is `mvp-implementation`.
- The latest local work adds Manage sidebar Chat/Docs product links, fixed-height non-scrollable sidebar behavior, and general one-time cross-app session handoff for Manage-to-Chat and Manage-to-Docs new-tab navigation. Browser/Compose acceptance is pending.
- The latest local work implements Chat Conversation Slices A and B: persisted audit-backed chat sessions, user-scoped session list/history endpoints, bounded same-session question condensation, transcript-oriented `chat-web` with stable `sessionId` propagation, and progressive frontend SSE rendering with partial-answer rollback on interrupted streams. User-owned browser/Compose acceptance is pending.
- The previous local work implements document creation/editor fixes plus the follow-up management editor group-selection, TipTap `3.25.0` toolbar/color controls, and cross-app docs links opening in new tabs. Query-time multimodal RAG remains approved and its slice-based implementation plan is written.
- Document image slice 1 is implemented and the user confirmed image storage through the local MinIO browser on 2026-06-01.
- Text-first RAG image indexing is implemented and RAG verification passed on 2026-06-01.
- Management editor published-document fallback is implemented and manage-web verification passed on 2026-06-01.
- Query-time multimodal RAG is approved and planned for implementation. Do not send every document image to OpenAI; select only images associated with final retrieved chunks and enforce the initial caps of 3 images and 5 MB total image bytes per chat request.
- Interim manual service updates can use `updateService.sh` from the repository root on the Linux demo host. This is a temporary source-build path until GHCR/CD is implemented.
- Demo-host boot recovery can use `installServiceAutostart.sh` from the repository root on the Linux demo host to install `advanced-rag.service`.
- The demo host deployment branch should remain aligned with `origin/mvp-implementation`; host-only generated data such as `backups/` belongs outside tracked Git state.
